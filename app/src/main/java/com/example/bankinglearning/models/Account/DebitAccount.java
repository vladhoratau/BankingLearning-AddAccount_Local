package com.example.bankinglearning.models.Account;

public class DebitAccount extends BaseAccount {
    public DebitAccount(String accountNo, Double balance) {
        super(accountNo, balance);
    }
}
