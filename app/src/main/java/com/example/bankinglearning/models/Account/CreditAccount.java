package com.example.bankinglearning.models.Account;

public class CreditAccount extends BaseAccount {
    public CreditAccount(String accountNo, Double balance) {
        super(accountNo, balance);
    }
}
