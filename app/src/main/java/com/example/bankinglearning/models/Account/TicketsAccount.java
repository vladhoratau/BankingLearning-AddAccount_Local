package com.example.bankinglearning.models.Account;

public class TicketsAccount extends BaseAccount {
    public TicketsAccount(String accountNo, Double balance) {
        super(accountNo, balance);
    }
}
